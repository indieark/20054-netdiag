# API 与接口说明

> 本文件维护项目 API、schema、事件合同和跨模块接口。没有 API 时可以保留为空索引。

## 接口列表

| 接口/模块 | 职责 | 说明 |
|---|---|---|
| - | - | 按项目实际情况填写 |

## 数据结构

<!-- 记录稳定 schema、类型约束和兼容性规则。 -->

## 变更规则

- API 或 schema 变更应写清兼容性影响。
- 计划阶段的草案写入 `plans/`；确定后的长期合同同步到本文件或对应专题文档。
